export {
  dirname,
  fromFileUrl,
  parse,
  join
} from 'https://deno.land/std@0.139.0/path/mod.ts'

// const versions = { latest: '', default: 139, path: 139 }

// export const { dirname, fromFileUrl, parse, join } = await import(
//   `https://deno.land/std@0.${versions.path}.0/path/mod.ts`
// )
